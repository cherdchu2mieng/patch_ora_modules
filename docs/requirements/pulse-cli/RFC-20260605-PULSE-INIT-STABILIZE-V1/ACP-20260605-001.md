<!-- MANDATORY: All updates and additions to this document MUST strictly follow the /build-rfc skill methodology. -->
# Acceptance Test Plan: ACP-20260605-001

## 1. Document Control
- **RFC ID**: RFC-20260605-PULSE-INIT-STABILIZE-V1
- **Project**: pulse-cli
- **Patch Workspace**: /home/a2it49072/ghq/github.com/cherdchu2mieng/patch_ora_modules
- **Date**: 2026-06-05

## 2. Test Scenarios (Integration)

### Scenario 1: Simplified Init Flow (User Scope)
- **Action**: รันคำสั่ง `pulse init` หรือ `bun run src/pulse.ts init`
- **Input**: เลือกขอบเขต `u` (User) และกด Enter เพื่อใช้ค่า Default ในคำถามที่เหลือทั้งหมด
- **Expected Result**:
    - สร้างไฟล์ `pulse.config.[username]_[ai-project-number].json` สำเร็จ
    - ภายในคอนฟิก ค่า `org` ตรงกับชื่อ User, และ `projectNumber` ตรงกับโปรเจกต์ของ AI Board
    - ส่วนของ `board.AIB.repo` ต้องมีค่าเป็น `[username]/pulse-oracle`

### Scenario 2: Simplified Init Flow (Org Scope)
- **Action**: ลบไฟล์คอนฟิกเดิม และรันคำสั่ง `pulse init` อีกครั้ง
- **Input**: เลือกขอบเขต `o` (Org) และกด Enter เพื่อใช้ค่า Default ทั้งหมด
- **Expected Result**:
    - สร้างไฟล์ `pulse.config.itinfosv_1.json` สำเร็จ
    - ค่า `org` ต้องเป็น `itinfosv` และ `projectNumber` ต้องเป็น `1`
    - โครงสร้าง `board` ถูกบันทึกเป็น Object สมบูรณ์

### Scenario 3: Blog Type Compatibility (TypeError Fix)
- **Action**: แก้ไขไฟล์ `pulse.config.json` ให้ส่วนของ `board.ITB` และ `AIB` เป็นรูปแบบ Object 
- **Action**: รันคำสั่งประกาศข่าว เช่น `pulse blog <path-to-any-md-file>`
- **Expected Result**:
    - คำสั่งต้องทำงานสำเร็จ ไม่เกิด `TypeError: blogRepo.includes is not a function`
    - โพสต์ถูกสร้างขึ้นบน GitHub Discussion ของ IT Master Board ได้อย่างถูกต้อง

### Scenario 4: Cross-Board Handover Stability (chb fix)
- **Action**: เตรียม Issue 1 รายการบน IT Master Board
- **Action**: รันคำสั่ง `pulse chb <id>` (จากมุมมองของบอร์ดหลัก) เพื่อส่งต่องานไปยัง AIB
- **Expected Result**:
    - ระบบแจ้งเตือน "Delegating task..."
    - งานถูกสร้างขึ้นบนโปรเจกต์ของ AIB โดยไม่มี Error เรื่อง Oracle constraint (เช่น Option not found for field "Oracle")
    - ลิงก์เชื่อมโยงไปกลับ (Bidirectional Link) ถูกบันทึกลงในช่อง Anchor สมบูรณ์

## 3. Final Verification Status
- [ ] Scenario 1: Passed
- [ ] Scenario 2: Passed
- [ ] Scenario 3: Passed
- [ ] Scenario 4: Passed

---
**Oracle Signature**: Gemi 🌊 (Deep Blue Horizon)
