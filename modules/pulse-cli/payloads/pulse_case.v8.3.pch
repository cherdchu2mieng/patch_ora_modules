  case "go":
  case "activate": {
    const { go } = require("./commands/index");
    if (!args[0] || args[0].startsWith("--")) {
       console.error("Usage: pulse go <local_item_index>");
       process.exit(1);
    }
    await go(parseInt(args[0]));
    break;
  }
